---
# ─────────────────────────────────────────────────────────────────────────────
# Copy this whole folder to src/content/projects/<your-slug>/ and edit it.
#
# The FOLDER NAME IS THE SLUG and it is PERMANENT — it appears in the URL and in every link anyone
# ever shares. Lowercase, hyphens, no year suffix unless the project genuinely recurs annually.
# It does not have to match the GitHub repo name; prefer a readable slug (school-riscv, not
# project-schoolRISCV).
#
# Required: title, summary, year, status, tags. Everything else is optional.
# A bad value fails the build with a message naming this file — that is deliberate, and the
# message tells you what to fix.
# ─────────────────────────────────────────────────────────────────────────────

# Max 70 characters.
title: <Project title>

# 40–200 characters. Reused as the meta description and inside the generated social-preview image,
# which is why the length is bounded. Write it as one sentence a stranger would understand.
summary: <One sentence describing what this is and why it is interesting.>

# Optional, but please try. Commit the ORIGINAL image next to this file — do not pre-resize it;
# the build produces better variants than a manual export. Without a cover, a generated stub
# renders and the row list stays uniform — but the stub solves *uniformity*, not *emptiness*.
# One photo of the thing working is worth an hour of somebody's time.
# cover: ./cover.jpg
# coverAlt: <What is in the photo. Describes the content, not "photo of…".>

year: 2026
semester: spring # spring | fall | summer — optional
status: in-progress # idea | in-progress | completed | archived

# 1–6 tags, all of which must exist in src/data/tags.yaml. A build error will suggest the nearest
# match if you typo one.
tags: [fpga]

# Set true only if this should appear on the club homepage.
featured: false

# `person` is an id from src/data/people.yaml. A one-off contributor can be credited by `name`
# alone — nobody has to be added to people.yaml to be thanked.
team:
  - { person: m-kuskov, role: Instructor }
  - { name: <Your name>, role: RTL }

# YouTube, Rutube, VK Video or Dzen — any of the four. Nothing is loaded from the provider until a
# visitor clicks play.
# video: https://rutube.ru/video/<id>/
# videoTitle: <What the video shows>

# Images live next to this file. `alt` is required on every one.
# gallery:
#   - { src: ./datapath.png, alt: Block diagram of the single-cycle datapath, caption: Single-cycle datapath }
#   - { src: ./board.jpg, alt: The board wired to a logic analyzer }

links:
  repo: https://github.com/InnoChipDesign/<repo>
  # docs: …
  # paper: …

stack: [SystemVerilog, Vivado]
hardware: [Digilent Basys 3]

# awards:
#   - { title: <Placement>, event: <Competition>, year: 2026 }

# Keep this true until the page is finished. A draft has no URL and is not searchable, so nothing
# half-written can be shared by accident.
draft: true
---

{/*
  Use ## and ### only. `#` is reserved for the page title, which the layout renders.
  The four headings below are a suggestion, not a rule — but they are the shape that reads best.
*/}

## The problem

## How it works

## Results

## What's next
